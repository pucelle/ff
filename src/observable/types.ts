/** 
 * Indicates that the variable with current type is observable.
 * It equals assigning `@observable` to a class, and instantiate it to get a instance object.
 */
export type Observable<T extends object> = T