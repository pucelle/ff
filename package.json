{
	"name": "ff",
	"version": "1.0.0",
	"description": "A small JavaScript utility library, to make web programming easier.",
	"main": "out/index.js",
	"scripts": {
		"compile": "tsc -b",
		"watch": "tsc -b -w",
		"test": "npm run test-base && npm run text-dom",
		"test-base": "jest",
		"test-dom": "gulp test && start test/dom/index.html",
		"postinstall": "cd test/dom && npm install"
	},
	"author": "pucelle",
	"license": "MIT",
	"devDependencies": {
		"@types/jest": "^24.0.9",
		"browserify": "^16.2.3",
		"chai": "^4.2.0",
		"exorcist": "^1.0.1",
		"gulp": "^4.0.0",
		"gulp-util": "^3.0.8",
		"jest": "^24.1.0",
		"mocha": "^6.0.2",
		"ts-jest": "^24.0.0",
		"tsify": "^4.0.1",
		"typescript": "^3.6.0",
		"vinyl-source-stream": "^2.0.0",
		"watchify": "^3.11.1"
	}
}