# ChangeLog


# 2.0.2

- `Interval.flush` will not reset if canceled.
- `SizeUtils.formatSize` support `fixedPoint` notation.
- `ListUtils.indexBy` -> `ListUtils.indexToMap`, and add a `ListUtils.indexToObject`.
- `ListUtils.groupBy` -> `ListUtils.groupToMap`, and add a `ListUtils.groupToObject`.


# 2.0.1

- Exclude useless files.
- Update dependencies.